# use_request showcase

This Flutter app is the complete interactive showcase for `use_request`. It
covers automatic and manual requests, search, concurrency controls, retry,
polling, cache, pagination, and advanced options.

```bash
flutter pub get
flutter run -d chrome
```

The small copyable examples live in [`../example`](../example). The showcase
is excluded from the published package archive.
